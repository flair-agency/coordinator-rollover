# coordinator-rollover

A Codex Skill for replacing a long-running coordinator with a compact,
evidence-backed handoff. The first version runs only when a user requests it;
it does not diagnose degradation or archive tasks automatically.

The repository root is the installable Skill directory. Review
[`SKILL.md`](SKILL.md) for the operating contract.
